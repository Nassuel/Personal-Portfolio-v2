import type { Config } from "prettier";

const config: Config = {
    arrowParens: "avoid",
    singleQuote: false,
    bracketSpacing: true,
    endOfLine: "lf",
    semi: true,
    tabWidth: 4,
    trailingComma: "none"
};

export default config;
